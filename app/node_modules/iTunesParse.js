var fs = require("fs"),
    itunes = require("itunes-data"),
    parser = itunes.parser();
    // storedb = require("../bower_components/storedb"),
    // stream = fs.createReadStream("/users/adamseckel/Music/iTunes/iTunes\ Music\ Library.xml");

// var songs = []
// db.addOverlay('local');

// parser.on("track", function(track) {
//   songs.push(track);

// });

// parser.on("album", function(album) {
//     console.log("album:", track);
// });

// stream.pipe(parser);

// stream.on('end', function () {console.log(songs)});

// stream.on('end', function () {
//   var outputFilename = '/users/adamseckel/desktop/my.json';

//   fs.writeFile(outputFilename, JSON.stringify(songs, null, 4), function(err) {
//     if(err) {
//       console.log(err);
//     } else {
//       console.log("JSON saved to " + outputFilename);
//     }
//   });
// })




